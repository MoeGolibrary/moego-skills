# Cursor Adapter

Cursor 支持通过 `.cursorrules` 或项目配置加载自定义规则。

## 配置方式

在项目根目录创建 `.cursorrules` 文件，引用 skill 内容：

```
# MoeGo E2E Testing Rules

[包含 skill.md 的内容]
```

## 未来计划

计划开发自动转换工具，将 skill.md 转换为 Cursor 兼容格式。
